# Benchmark Smoke Runners

Smoke runners are fast development checks. They are useful before a full
benchmark, but their aggregate scores should not be used as method evidence.

Entrypoint:

```bash
uv run python -m benchmarks.smoke.run_subset
```
