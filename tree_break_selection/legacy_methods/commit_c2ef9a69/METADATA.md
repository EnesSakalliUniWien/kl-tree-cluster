# Tree-Break Selection Legacy Method Snapshot c2ef9a69

This package is a full copy of `tree_break_selection/` from commit
`c2ef9a69e0888168950bdee4a41ae8ab9996e32f`.

The snapshot is nested under:

`tree_break_selection.legacy_methods.commit_c2ef9a69.tree_break_selection`

Absolute imports inside the copied files were mechanically rewritten from
`tree_break_selection...` to this nested namespace so the old method can be
imported in the same Python process as the current implementation.

Purpose: diagnostic old-versus-current method comparisons. This is not a
production code path.
