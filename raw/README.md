# Raw Sources

`raw/` stores immutable source material for the project wiki.

New documents enter through `raw/inbox/`. Keep original wording and filenames
as stable as practical. Derived summaries belong in `wiki/`, not here.

Use `raw/assets/` for copied figures, PDFs, screenshots, or other binary
evidence that supports a wiki page.
