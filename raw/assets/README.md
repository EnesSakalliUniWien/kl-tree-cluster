# Raw Assets

This directory holds small, curated evidence files copied from generated or
external sources when wiki pages or analysis notes need stable source paths.

Do not use this directory for live benchmark output. Benchmark commands should
write generated runs under ignored output directories such as
`benchmarks/results/`; only promote the minimal files needed as durable
evidence.
